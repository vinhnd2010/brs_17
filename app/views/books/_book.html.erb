<tr>
  <td><%= link_to book.title, book %></td>
  <td><%= book.author %></td>
  <td><%= book.num_pages %></td>
  <td><%= book.publish_date.strftime t("book.date_format") %></td>
  <td><%= book.category.name %></td>
</tr>
