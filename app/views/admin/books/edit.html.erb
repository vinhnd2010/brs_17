<% provide :title, t("book.edit", title: @book.title) %>

<div class="row">
  <hr>
  <div class="col-md-8 col-md-offset-2">
    <h1 class="text-info text-center">
      <%= t "book.edit", title: @book.title %>
    </h1>
    <br>
    <%= render "form" %>
  </div>
</div>
